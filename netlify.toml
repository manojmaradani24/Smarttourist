[build]
  publish = "dist"
  command = "npm run build"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# proxy API requests to backend (replace with actual backend URL later)
[[redirects]]
  from = "/api/*"
  to = "https://BACKEND_URL/:splat"
  status = 200
  force = true
